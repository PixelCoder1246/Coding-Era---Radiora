# NOTE: This file is NOT used by the deployment.
#
# The nginx config is written directly to /etc/nginx/conf.d/radiora.conf
# on the EC2 by deploy.sh at deploy time (with real domain names filled in).
#
# This file is kept here as a reference/documentation only.
# See deploy.sh for the actual template that gets written to the system.
